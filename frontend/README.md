# ALTIQ AI Frontend

## Setup

```bash
npm install
npm run dev
```

The Vite dev server runs on http://localhost:5173 and proxies `/api` requests to the backend at http://localhost:5000.

## Logo

Place the official logo at `public/altiq-logo.svg`. Until then a simple orbital mark is used automatically.
