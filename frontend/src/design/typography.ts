/**
 * ALTIQ AI Typography
 * Geist  → hierarchy (logo, headings, nav, buttons, labels, cards, UI)
 * Montserrat → body (paragraphs, forms, descriptions, AI output, long-form)
 * No Inter. No system-ui. No generic sans-serif in the design system.
 */
export const fonts = {
  heading: 'Geist',
  body: 'Montserrat',
} as const;
